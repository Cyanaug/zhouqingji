# PROGRESS —— 公开版建设日志

> 这里只记录可公开复用的功能演进。作者作品、阅读规模、具体批次、机器路径与个人环境不进入公开仓库。

## 2026-08-30 · v1.7 掌中册

- 设置页可临时开启局域网只读入口，手机扫码即看；入口默认关闭，随机配对口令逐次更换，停止或退出后失效。
- 新增可安装 PWA 与 Tailscale Serve 私密 HTTPS 路线；最新移动快照可离线阅读，手机收藏、阅读足迹和私人随记独立保存。
- 新增自包含单 HTML 留影，不依赖外部网页资源；移动快照由程序自动生成，不要求用户维护第二份 JSON 或专用包格式。
- 二维码在本机生成，移动接口拒绝写入；公开发行仍不包含任何 corpus、results 或个人设置。完整教程见 `MOBILE_ACCESS.md`。

## 2026-08-10 · v1.6 多工具接手、账本与更新链加固

- 新增工具中立 `AGENTS.md`；`CLAUDE.md` 导入同一基线。Codex 与 Claude Code 各保留原生 skills/agents，AGY、CodeBuddy 复用独立 task 协议。
- reads/votes/thread 写入增加跨进程锁；投票同值重试幂等、冲突整批拒收，best 使用稳定 ballot_id。新增只读数据审计器。
- Git clone 继续安全快进；ZIP 安装支持固定官方仓库、大小/路径/链接校验、发行允许清单、覆盖前备份与失败回滚。corpus、results、settings、batches 永不由更新器覆盖。
- 新增统一检查脚本、ZIP 更新测试与 GitHub Actions；启动器移除本机绝对路径。
- 公开 `main` 定义为稳定发行通道；版本 tag 必须与 `VERSION` 一致，通过完整检查后自动创建 GitHub Release。

## 2026-07 · v1.5 评论质量与跟帖机械

- 点赞批量票面加入 `best` 相对判断；方向票与 best 分开统计。
- 批量装箱同时受条数和字符预算约束；已投过和本人评论按投票人逐项裁剪。
- 跟帖区分一楼广度与深楼讨论；优先邀请相关发言者，默认避免同人重复回应同一层。
- 新增 `nextround` 候选提案、逐字引用校验、沉默回执与 void 级联隐藏。

## 2026-07 · v1.4 本地设置与可维护性

- 设置页集中管理站点文字、默认视图、评分口径、端口、阅读文体和派发偏好。
- 默认人设与用户侧车分离；用户新增、改写、隐藏的人设存放在 corpus 下，不随更新覆盖。
- 设置页加入版本显示、检查更新和 Git 安全快进。
- 评论列表加入模型/读者/分数筛选，跟帖树支持分支折叠。

## 2026-07 · v1.3 多文体与校准防偏

- 非诗文体使用“换判据、不换人格”的提示层，并由用户显式加入阅读池。
- 非诗作品与诗歌榜单分离，避免不同判断标准混在一条校准量表。
- 校准加入样本量收缩、诗池失衡诊断与信任因子，低样本或分配偏斜时自动靠近恒等映射。

## 2026-07 · v1.1–v1.2 侧车与讨论模式

- 增加分段、偏爱、策展、设置、人设、校准等 sidecar；作品正文与 append-only 阅读账本保持独立。
- 建成跟帖与点赞两条非评分信号通道；它们不进入盲读榜单或校准。
- 前端加入读者墙、时间轴、统计、讨论树和评论质量徽标。

## 2026-07 · v1.0 最小闭环

- 建立 corpus / theater / results 三层边界。
- 完成盲读任务生成、独立回执、全批校验、append-only 入库与 coverage 计算视图。
- 完成本地 Python 服务器与无构建前端，展示作品、读者反应、榜单和时间轴。

## 公开路线图

- 跑法/批次中心是否进入 webapp。
- 深跟帖的 token 与沉默终止阈值如何基于真实试点校准。
- 是否拆分体积较大的状态 API。
- 校准高级阈值是否应暴露给普通用户。
