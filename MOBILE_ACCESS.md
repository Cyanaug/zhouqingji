# 昼青集上手机：三种方式

手机功能不需要安装前端框架，也不需要把作品上传到公网。电脑仍是作品与评论的唯一真源；手机拿到的是一份只读移动快照，自己的收藏、阅读足迹和私人随记另存在手机浏览器里。

## 最推荐：家中同一 Wi-Fi，临时扫码

电脑端：

1. 启动昼青集，在顶栏打开「设置」。
2. 找到「手机访问」，点「开始手机访问」。
3. 如果 Windows 第一次询问网络权限，只允许“专用网络”，不要允许“公用网络”。
4. 手机与电脑连同一个家用 Wi-Fi，用手机相机扫连接签上的二维码。
5. 看完回到电脑点「停止」，或直接关闭昼青集。入口和本次口令会一起失效。

这种方式不要求手机安装任何东西，普通浏览器就能看。它使用局域网 HTTP，适合家中等可信网络；不要在酒店、学校访客网、咖啡店等陌生公共 Wi-Fi 开启。某些路由器会隔离无线设备，如果扫码打不开，可先关闭手机流量、确认两台设备在同一 Wi-Fi，再试连接签上的备用地址。

局域网 HTTP 可以正常阅读，但浏览器通常不会提供完整的“安装应用”与离线缓存能力。这是浏览器的安全限制，不是昼青集故障。

## 可选：Tailscale 私密 HTTPS，异地访问并安装 PWA

Tailscale 是可选通道，不是昼青集依赖。它在你的设备间建立私密网络；作品仍从你自己的电脑读取，不会发布成可被搜索引擎抓取的网站。昼青集也不会调用 Funnel，因为 Funnel 会把服务公开到互联网。

准备一次即可：

1. 在电脑和手机安装 Tailscale，并登录同一个 Tailscale 网络。
2. 电脑启动昼青集并点「开始手机访问」，默认手机端口是 `8738`。
3. 在 Windows 管理员终端运行 `tailscale serve 8738`。保持终端打开；它会显示一个 `https://……ts.net` 地址。
4. 回到昼青集「设置 → 手机访问」，展开“已经开启 Tailscale Serve？”，粘贴这个 HTTPS 地址并生成私密二维码。
5. 手机保持 Tailscale 已连接，扫码打开。浏览器菜单中选择“安装应用”或“添加到主屏幕”。

不用时，同时停止昼青集的手机入口和 Tailscale Serve。前台运行时，在命令所在终端按 `Ctrl+C` 即可；若你改用过后台模式，可运行 `tailscale serve reset` 清掉 Serve 配置。

Tailscale 官方教程：<https://tailscale.com/docs/features/tailscale-serve>

## 完全离线：导出一个 HTML 留影

在「设置 → 手机访问」点「导出离线 HTML」，会得到一个文件，里面已经包含当时的作品、评论、样式和程序。把它通过数据线、局域网传输或你信任的方式送到手机，点击后用浏览器打开即可。

- 优点：不开放网络、不需要服务器，文件就是当时的完整留影。
- 限制：电脑内容变化后不会自动更新，需要重新导出；不同手机浏览器对本地 HTML 的收藏和随记保存支持不一致。
- 它不是 APK，但用途接近一个只读备份应用。把 HTML 再套一层 WebView 可以生成 APK，不过会增加签名、安装包更新和系统兼容成本，却不会让静态内容自动同步，因此当前不作为主路线。

## 手机上哪些东西会保留

通过局域网或 Tailscale 打开的移动版会在手机本地保存：

- 你在手机单独标记的收藏；
- 最近读过哪些作品；
- 每首作品下的私人随记；
- 最近一次成功收到的电脑快照，断网后仍可继续翻阅（需要 HTTPS/PWA 环境时最可靠）。

电脑下次生成新快照时，只更新作品和评论，不覆盖上述手机记录。移动版“掌中册”页面可以导出或导入一份手机记录备份；清理浏览器网站数据、卸载浏览器或更换手机前，建议先导出。

手机端目前刻意只读：不能改诗稿、删评论、改可见性或把手机随记写回电脑。这样可以守住 `corpus/诗稿.json` 这个唯一真源，避免两边同时编辑造成冲突。

## “移动快照 JSON”和 `.zqpack` 是什么

移动版内部确实用 JSON 表达“有哪些作品、评论与统计”，但它由程序自动生成：

- 临时访问时，它只存在于电脑内存和手机的私密浏览器存储中；
- 导出 HTML 时，它被嵌入同一个 HTML 文件；
- 你不需要寻找、编辑或手工更新任何 JSON 文件。

`.zqpack` 只是早期讨论过的“给快照包一个专用后缀”的设想。当前没有采用它，因为一个自包含 HTML 已能完成离线留影，额外发明文件格式只会要求手机再安装专用打开器。以后若真的需要附件、增量包或双向迁移，再考虑独立包格式更合适。

## 隐私边界

- 手机入口默认关闭，每次开启生成新的随机口令。
- 二维码在电脑本机生成，口令不会送往第三方二维码网站。
- 手机服务只提供观看接口，所有写入请求都会拒绝。
- 搜索引擎无法凭空发现家中局域网地址；Tailscale Serve 也只对同一私密网络内的设备开放。
- 口令不是公共 Wi-Fi 上的加密替代品：陌生网络用 Tailscale HTTPS，或改用离线 HTML。

