# 昼青集上手机：三种方式

手机功能不需要安装前端框架，也不需要把作品上传到公网。电脑仍是作品与评论的唯一真源；手机拿到的是一份只读移动快照，自己的收藏、阅读足迹和私人随记另存在手机浏览器里。

## 最推荐：家中同一 Wi-Fi，扫码连接

电脑端：

1. 启动昼青集，在顶栏打开「设置」。
2. 找到「手机访问」，点「开始手机访问」。
3. 如果 Windows 第一次询问网络权限，只允许“专用网络”，不要允许“公用网络”。
4. 手机与电脑连同一个家用 Wi-Fi，用手机相机扫连接签上的二维码。
5. 平时用完可回电脑点「停止」。如果只是偶尔看一次，保持默认的一次性入口即可。

这种方式不要求手机安装任何东西，普通浏览器就能看。它使用局域网 HTTP，适合家中等可信网络；不要在酒店、学校访客网、咖啡店等陌生公共 Wi-Fi 开启。某些路由器会隔离无线设备，如果扫码打不开，可先关闭手机流量、确认两台设备在同一 Wi-Fi，再试连接签上的备用地址。

扫码后打开的是手机默认浏览器中的昼青集页面，不会自动生成 APK。第一次打开后，可以收藏当前页面，或在浏览器菜单中选择“添加到主屏幕”建立快捷入口。

- 默认的一次性入口：只要电脑这一次的手机入口仍开着，之后点书签或快捷图标就能回来；停止、退出或重开后需要重新扫码。
- 可选的“信任这台手机”：开始前勾选后，会保存一张 30 天连接签；昼青集下次启动会自动恢复同一口令，通常不必重新扫码。停止按钮会变成“停止并撤销”，撤销后旧签立即失效。

“信任这台手机”是便于理解的界面名称，技术上信任的是二维码里的连接签，而不是手机硬件身份。任何拿到完整二维码的人都能在有效期内使用，所以仍不要转发。

它不是一条始终占用网络的长连接：打开页面或点“现在更新”时才从电脑拉取最新快照，平时阅读已经拉到手机中的内容。所谓“关闭”，指电脑不再监听手机端口、这轮口令失效；手机上已经打开的页面不会被远程强行关掉，但刷新或再次拉取会失败。

局域网 HTTP 可以正常阅读，也可以建立普通主屏幕快捷方式，但浏览器通常不会把它安装成完整离线 PWA。这是浏览器的安全限制，不是昼青集故障。

## 手机和平板兼容性

- Android 手机/平板：用系统相机、扫码工具或微信等识别二维码后，选择 Chrome、Edge、Firefox 等现代浏览器打开。
- iPhone/iPad：用系统相机扫码，使用 Safari 最稳妥；若先进入系统“扫码器”预览，可再点“在 Safari 中打开”。连接签会保留在完整地址里，转到 Safari 或“添加到主屏幕”仍能配对。旧版曾会过早隐藏连接签；由旧版建立的无签主屏幕图标需删除后重新扫码添加。
- 使用“信任这台手机”时，安装清单的启动地址也会携带这张有效连接签，因此 iPad 从主屏幕独立启动时不依赖 Safari 是否共享网站存储。停止并撤销后，主屏幕图标仍在，但旧签立即失效。
- 若完整地址仍打不开：到“设置 → 隐私与安全性 → 本地网络”确认 Safari 或所用浏览器获准访问，并确认电脑与 iPad 在同一 Wi-Fi、电脑的手机入口仍开着。
- 只要设备能访问电脑给出的局域网地址，页面会按手机或平板宽度自适应。扫码工具本身不决定功能，真正保存书签、快照和随记的是最终打开页面的那个浏览器。
- 手机热点也可代替家用 Wi-Fi：让电脑连到手机热点后再扫码。少数热点或校园网启用了“设备隔离”，此时两台设备虽然显示在同一网络，也可能互相访问不到。

## 可选：Tailscale 私密 HTTPS，异地访问并安装 PWA

Tailscale 是可选通道，不是昼青集依赖。它在你的设备间建立私密网络；作品仍从你自己的电脑读取，不会发布成可被搜索引擎抓取的网站。昼青集也不会调用 Funnel，因为 Funnel 会把服务公开到互联网。

准备一次即可：

1. 在电脑和手机安装 Tailscale，并登录同一个 Tailscale 网络。
2. 电脑启动昼青集并点「开始手机访问」，默认手机端口是 `8738`。
3. 在 Windows 管理员终端运行 `tailscale serve 8738`。保持终端打开；它会显示一个 `https://……ts.net` 地址。
4. 回到昼青集「设置 → 手机访问」，展开“已经开启 Tailscale Serve？”，粘贴这个 HTTPS 地址并生成私密二维码。
5. 手机保持 Tailscale 已连接，扫码打开。浏览器菜单中选择“安装应用”或“添加到主屏幕”。

不用时，同时停止昼青集的手机入口和 Tailscale Serve。前台运行时，在命令所在终端按 `Ctrl+C` 即可；若你改用过后台模式，可运行 `tailscale serve off` 停止共享。

Tailscale 不是同步盘。它只把“手机如何安全找到这台电脑”换成私密 HTTPS；昼青集的更新方式仍是打开页面或点“现在更新”时拉取。电脑离线时，已安装的 PWA 会显示上次成功保存的留影。

Tailscale 官方教程：<https://tailscale.com/docs/features/tailscale-serve>

## 完全离线：导出一个 HTML 留影

在「设置 → 手机访问」点「导出离线 HTML」，会得到一个文件，里面已经包含当时的作品、评论、样式和程序。把它通过数据线、局域网传输或你信任的方式送到手机，点击后用浏览器打开即可。

- 优点：不开放网络、不需要服务器，文件就是当时的完整留影。
- 限制：电脑内容变化后不会自动更新，需要重新导出；不同手机浏览器对本地 HTML 的收藏和随记保存支持不一致。
- 它不是 APK，但用途接近一个只读备份应用。把 HTML 再套一层 WebView 可以生成 APK，不过会增加签名、安装包更新和系统兼容成本，却不会让静态内容自动同步，因此当前不作为主路线。

## 手机上哪些东西会保留

通过局域网或 Tailscale 打开的移动版会在手机本地保存：

- 你在手机单独标记的收藏；
- 最近读过哪些作品；
- 每首作品下的私人随记；
- 最近一次成功收到的电脑快照，断网后仍可继续翻阅（需要 HTTPS/PWA 环境时最可靠）。

电脑下次生成新快照时，只更新作品和评论，不覆盖上述手机记录。更新后“本次带回”会提示新增作品、评论/跟帖和修改作品的数量。

移动版“掌中册”页面可以导出或导入一份手机记录备份；它只是一个很小的 JSON 文件，下载到手机的“下载/文件”目录，不上传电脑，也不进入作品仓库。它保护的是收藏、足迹和随记，不是作品版本回滚。已有掌中记录但 30 天未备份时，页面会温和提醒一次。

浏览器会按“协议 + 地址 + 端口”隔离网站数据。因此 `http://192.168…:8738` 的普通扫码入口与 `https://……ts.net` 的 Tailscale 入口是两个独立保险格，彼此不会自动读到收藏和随记。这种隔离来自浏览器安全机制。要换入口时，先在旧入口“导出掌中记录”，再在新入口“导入掌中记录”，程序会按作品编号合并。

手机端目前刻意只读：不能改诗稿、删评论、改可见性或把手机随记写回电脑。这样可以守住 `corpus/诗稿.json` 这个唯一真源，避免两边同时编辑造成冲突。

## “移动快照 JSON”和 `.zqpack` 是什么

移动版内部确实用 JSON 表达“有哪些作品、评论与统计”，但它由程序自动生成：

- 临时访问时，它只存在于电脑内存和手机的私密浏览器存储中；
- 导出 HTML 时，它被嵌入同一个 HTML 文件；
- 你不需要寻找、编辑或手工更新任何 JSON 文件。

`.zqpack` 只是早期讨论过的“给快照包一个专用后缀”的设想。当前没有采用它，因为一个自包含 HTML 已能完成离线留影，额外发明文件格式只会要求手机再安装专用打开器。以后若真的需要附件、增量包或双向迁移，再考虑独立包格式更合适。

## 隐私边界

- 手机入口默认关闭，一次性入口每次开启生成新的随机口令；只有作者主动勾选时，才保存 30 天可信连接签。
- 二维码在电脑本机生成，口令不会送往第三方二维码网站。
- 手机服务只提供观看接口，所有写入请求都会拒绝。
- 搜索引擎无法凭空发现家中局域网地址；Tailscale Serve 也只对同一私密网络内的设备开放。
- 扫码不是公开搜索，但二维码里的随机口令本质上是一张“本轮只读门票”，并不是手机与电脑的一对一加密握手。同一局域网里拿到完整地址和口令的人，也能在口令失效前阅读快照。
- 家用 Wi-Fi 的实际风险通常很低；校园网仍可能有设备隔离、网络管理记录或 HTTP 被旁路观察的可能。移动入口拒绝全部写入，所以风险主要是作品、评论和随记被读到，而不是作品被改掉。
- 口令不是公共 Wi-Fi 上的加密替代品：陌生网络或在意传输保密时用 Tailscale HTTPS，或改用离线 HTML。
