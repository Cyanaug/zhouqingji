# 04 · 应用与设计 App & Design

架构**冻结**；视觉、排版、交互细节由当前实现方发挥——这是最该由实现工具判断的地方，请主动做好，改动记入 `NOTES.md`。

## 信息架构（页面与跳转）—— 架构 FROZEN

**v1 页面：**

1. **诗详情页（最重要、最难，力气优先砸这里）**：一首诗 + 它的读者反应（短跟帖列表）+ 一张**反应分布图**（这首诗在众多眼睛里的"形状"）。诗下方挂一个可折叠的"自注"块（`note`，仅作者可展开）。
2. **时间轴视图**：按 `date_written ?? created` 排列，作者能顺着自己这些年滚下来看。几乎免费（时间戳已在 schema）。
3. **榜单页**：见下，全是字段筛出的视图。
4. **深读专栏页**：容纳读者的 `long_form` 长文，从评论区最小化点入，慢慢读。

**v2 页面（钩子已留，纯加）**：论坛式 thread 视图（帖子 + 可顶可反驳的跟帖）、知情/初见对照、时序感悟分析面板。

## 诗，要在页面上"呼吸"—— 这是 v1 真正的难点

作者认可"网页不是简单东西"，但难的不是功能，是**诗的阅读体验**：

- 排版、留白、字体、间距，要让一首诗看着是美的；**评论不要去挤诗**。
- v1 只要把**诗详情页**做到"一首诗看着美、且你能看见它被怎么读"，最难的就过了；榜单、列表相对是机械活。
- **视觉定调（作者唯一硬偏好）**：稍微好看一点，**不要走全黑**。其余（配色、字体、动效、分布图长什么样）你来定，往雅致、克制、以诗为中心的方向走。

## 榜单 Boards —— 视图，且 RANKING RULE 冻结

榜单是**人类入口**（没人会一首首读 172 首，得有门面）。全部是按字段筛/排的视图，近乎零成本：

- **招牌榜**（综合最受欢迎，给人看的门面）
- **长诗榜 / 短诗榜**（按篇幅）
- **诗词榜**（按 genre）
- **最两极榜**（把读者劈成两半、方差最大的诗——往往正是作者边界玩得最狠、最好的几首）

诗词/长诗/短诗三个诗类专榜默认显示，但可在设置中统一关闭；已有的非诗文体榜也可逐项隐藏。开关只改变榜单首页入口，不删除作品、评分、阅读记录或直达页。

**铁律（FROZEN）**：所有榜单都**从诚实的单篇 blind 阅读里事后算出来**，**绝不让 LLM 去"排名/评最好最差"**。一旦叫模型排名，它就表演、迎合，读得就不真了。评分只是浅层信号，榜单是作者从分布派生的。

**注意用词**：作者提过"最差榜/最离谱榜"——**改做"最两极榜"**：同样的信号（读者分歧大），但不邀请读者去踩，而是标记多义、危险、可能最好的那几首。不做"最差"这种诱导贬低的榜。

跟帖索引只使用可审计字段：诗题/楼主搜索、作品、回复数、最大深度、最近活动与相应排序；从讨论详情返回时保留本次筛选，方便连续查看。楼层多不自动等于观点冲突，未定义可靠信号前不显示“激烈度”。

## GUI 必须给作者的动作（否则 corpus 只能手改文件，体验差）

- **切换可见性**：一键把某诗设 `public/private`（私人诗即时退出读者池与分享视图）。
- **剪自注**：在 `content` 里划取一段 → 移入 `note`（诗中收起、折叠展示、读者不可见）。见 01。
- **加背景小注**：给需要语境的诗填 `background`（只给读者看）。
- **作者藏印**：作者可给任何值得回看的盲读评论落一枚可撤回藏印。藏印只写 `results/curation.json` 侧车，不复制评论全文、不改变分数、榜单或校准；与 AI 同批横向产生的 `best` 加精分列展示，绝不混票。
- **过时提示**：作者改了某诗后，界面上把该诗"读的是旧版"的评论标出来（content_hash 不匹配）。

设置页的模型、执行通道和固定覆盖目标属于高级可选偏好，默认不写死供应商或历史通道。模型与通道留空时按当次运行环境确认；覆盖目标留空时先算当前最薄覆盖，再建议增加一层。

## v1 验收标准（做到这些就算 v1 成了）

作者打开应用，能：看见一首诗被排得很美、下面躺着几十个不同读者的真实短评 + 一张这首诗的反应分布图；能切到时间轴顺着年份读自己；能看一个招牌榜作为入口；能一键把某诗设私密、能把正文里的自注剪走。**盲读在后台按覆盖账稳稳堆厚，作者一句话推进一轮。** —— 这就把《夜路》那句"由世间所有的所见将它命名"兑现了九成。

## 移动阅读扩展（v1.7 / v1.7.1）

移动端沿用同一套原生 HTML/CSS/JS，不引入前端框架，也不另写一套应用。电脑的 `corpus` 与 `results` 始终是唯一真源；移动端只消费自动生成的只读快照。

- **局域网入口**：桌面作者端监听回环地址；作者显式开启后，另起 `0.0.0.0:8738` 只读服务。默认每轮使用新的随机配对口令；v1.7.1 可由作者主动保存 30 天可信连接签，应用重启自动恢复，撤销后立即失效。所有 POST 拒绝。
- **PWA**：manifest + service worker 只缓存应用壳；最新数据快照进 IndexedDB。局域网 HTTP 可浏览但通常不具备安装条件，Tailscale Serve 的私密 HTTPS 才是安装与可靠离线的推荐通道。
- **手机本地层**：收藏覆盖、最近阅读与私人随记按稳定 poem id 存在 localStorage，不进入电脑快照，所以内容更新不会冲掉它们；支持下载/导入这层记录的小型 JSON 备份，不写回电脑仓库。
- **更新差异**：新旧移动快照按稳定 poem/read id 与 content_hash 比较，在手机显示本次新增作品、评论/跟帖与正文变化，不改动任何本地记录。
- **单 HTML 留影**：把应用壳、样式、脚本、图标和快照内联为一个文件；不请求 CDN 或第三方二维码服务。它是静态留影，不冒充同步。
- **隐私最小化**：快照保留作者手机阅读所需的私密作品和评论，但移除设备 GUID、原始来源、人设编辑底稿、派发设置和本机端口；二维码由随包 MIT 组件在本机生成。
- **手机端不回写**：不提供改正文、可见性、评论或 corpus 侧车的接口。未来若做双向同步，必须先定义冲突、版本与认证协议，不能在现有只读接口上顺手开写。

## 评论回声与逐条校准（v1.7.2）

- 统计页的“读者回声簿”按人设汇总未折叠盲读评论收到的主动赞、踩、AI 加精与作者藏印。四种信号分列，不合成一个伪精确总分；回声受出场量和被投票量影响，不得解释为读者能力排名。
- “比例口径”默认收起，仅提供有明确分母的点赞率 `赞 ÷（赞＋踩）`，且至少 20 张方向票才进入排序。AI 加精是不同大小候选箱里的相对选择，现有账本没有统一曝光分母，因此不制造“加精率”。
- 评论卡同时保留原始分，并在已有正式校准结果时显示紧凑的“校 X.X”小签。这个数是该条评分按“人设 × 扮演模型”的一贯松紧映回参考分布后的结果；缺失时不猜值。
- 榜单“质分”不是逐条校准分的算术平均：逐条校准后还会做同模型相关折扣与诗级贝叶斯收缩，再用不改变名次的方差匹配恢复便于阅读的展示尺度。界面悬停同时说明展示质分、拉伸前校准贝叶斯与原始均分，避免把三个量混称为“校准分”。

## 词云按需索引（v1.7.2）

- 完整词频榜可切“出现次数”与“作品覆盖”；后者在同一首作品内无论重复多少次都只计 1，用来观察词跨作品出现的广度。词云本体仍按出现次数绘制，两个口径不偷换。
- 点击云中或榜中的词才请求词句索引，返回包含它的作品与诗句；首屏、普通作品页和联网手机快照都不预载索引。离线 HTML 只从已携带的诗正文即时查找。

普通用户步骤与网络权衡见 `MOBILE_ACCESS.md`。
